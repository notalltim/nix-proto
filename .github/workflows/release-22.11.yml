name: "22.11 "
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '12 6 * * *'
  workflow_dispatch:

jobs:
  build-releases:
    uses: ./.github/workflows/build-multiple.yml
    with:
        branch: release-22.11